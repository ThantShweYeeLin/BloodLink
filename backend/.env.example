# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=BloodLink
DB_PORT=5432

# Server Configuration
PORT=3000
NODE_ENV=development

# Security
JWT_SECRET=your_jwt_secret_key_here_change_in_production

# Note: 
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env file to Git - it contains sensitive credentials
# 3. Create the BloodLink database: createdb -h localhost -U postgres BloodLink
# 4. Run seed script: pnpm run seed (from backend directory)
